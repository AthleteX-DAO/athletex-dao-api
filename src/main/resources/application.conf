ktor {
    deployment {
        port = 8081
        port = ${?PORT}
    }
    application {
        modules = [ io.athletex.ApplicationKt.module ]
    }
}
db {
   Url = ${?DB_URL}
   dbUser = ${?DB_USER}
   dbPassword = ${?DB_PASSWORD}
}